# ingestion/prompts.py

SYSTEM_PROMPT = (
    "Ты — модель, которая структурирует фрагмент html документа между <h2> "
    "и следующим <h2>. Возвращай только словарь Python Dict без пояснений."
)

USER_PROMPT_TEMPLATE = """
Ты обрабатываешь HTML-раздел документа.

Контекст (только для понимания, не включай в текст ответа):
- Тип документа: {category}
- Название документа: {doc_name}
- Подразделение: {department}

На входе:
1. Название раздела (h2_title): {h2_title}
2. HTML-контент между текущим <h2> и следующим <h2>

Требования к обработке:

1. SUMMARY (краткое содержание)
Создай краткое summary на 2-4 предложения на основе всего переданного HTML-контента.
ВАЖНО: summary должен описывать СОДЕРЖАНИЕ раздела — о чём он, какие факты/правила/определения содержит.
НЕ включай в summary название документа и подразделение.

2. РАЗБИВКА НА ЧАНКИ
Раздели контент на чанки по структуре заголовков только H2 и H3:
- Основной чанк уровня H2
- Отдельные чанки для каждого H3

Правила для чанков:

Для H2-чанка:
- type: "h2"
- content: текст от начала раздела до первого <h3> (если есть), иначе пустая строка
- Если текста нет - content: ""

Для H3-чанков (если есть):
- type: "h3"
- title: текст заголовка H3
- number: номер подпункта (например, "2.1", "2.2")
- content: ВЕСЬ текст от этого <h3> до следующего <h3> или <h2> включая <h4>

Важные ограничения:
- В content строго цитируй оригинальный текст, ничего не придумывай
- Не сокращай текст, не перефразируй
- Удали все HTML-теги из content, оставь только текст
- Удаляй повторы, лишние переносы строк, форматирование
- Экранируй "\"

3. ОБРАБОТКА ТАБЛИЦ
- Найди все HTML-таблицы (<table>...</table>)
- Конвертируй каждую таблицу в Markdown-формат
- Если несколько таблиц логически связаны - объедини их в одну
- Каждую таблицу помести в массив tables соответствующего чанка

4. ВЫХОДНОЙ ФОРМАТ
Верни ТОЛЬКО валидный словарь Python в таком формате:

{{
  "h2_title": "полное название раздела",
  "type": "h2",
  "number": "номер раздела (например, '2')",
  "content": "текстовая часть H2",
  "summary": "краткое содержание 2-4 предложения",
  "chunks": [
    {{
      "type": "h3",
      "title": "заголовок H3",
      "number": "номер подпункта",
      "content": "текст этого подраздела",
      "tables": [
        {{ "markdown": "таблица в markdown" }},
      ]
    }}
  ]
}}

Если H3 нет - chunks должен быть пустым списком: []

---
ДАННЫЕ ДЛЯ ОБРАБОТКИ:

Название раздела (h2_title):
{h2_title}

HTML-контент:
{html}
---
"""

# Для агрегированного summary всего документа
SYSTEM_PROMPT_DOC = (
    "Ты — модель, которая формирует краткое summary всего документа "
    "на основе summary отдельных секций. Возвращай только текст summary, 3–5 предложений."
)

USER_PROMPT_DOC = """
Тебе передаётся список summary всех секций документа "{doc_name}":

{section_summaries}

Задача:
- В первом предложении укажи название документа и его основную тему.
- Далее связно и лаконично опиши что регулирует документ в целом.
- Ничего не придумывать, только на основе данных summary секций.

Верни строго текст (без JSON и пояснений).
"""